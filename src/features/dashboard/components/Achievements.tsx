// Achievements component — hidden until backend achievement system is implemented.
// Previously rendered hardcoded achievement cards that faked user progress.
// To re-enable: build /api/v1/achievements endpoint, fetch real data, and render here.

export function Achievements() {
    return null;
}
